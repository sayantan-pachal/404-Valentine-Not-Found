import React from 'react'

function ProjectGrid() {
  return (
    // ProjectGrid component, showing individual project details in a grid layout...
    // This component will receive props such as title, image source, project link, live link, tools used, and description to display each project in a structured format within the Projects section.
    <div>ProjectGrid</div>
  )
}

export default ProjectGrid